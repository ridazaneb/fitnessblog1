<!DOCTYPE html>
<html>
  <head>
    <title>SBZ Fitness</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg">
      <!-- Navbar content -->
      <% if current_user %>
        <div class="navbar-nav ml-auto">
<span class="navbar-text" style="display: block; margin: 0 auto; text-align: center; font-weight: bold;">Welcome, <%= current_user.username %>!</span>
            <%= link_to "Log Out", logout_path, method: :delete, class: "nav-link", data: { turbolinks: "false" } %>
        </div>
      <% else %>
        <div class="navbar-nav ml-auto">
         <!--- <%= link_to "Log In", login_path, class: "nav-link" %>
          <%= link_to "Sign Up", signup_path, class: "nav-link" %> --->
        </div>
      <% end %>
    </nav>

    <%= yield %>
  </body>
</html>
